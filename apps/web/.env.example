# Server-only SendGrid credentials for the portfolio contact form.
SENDGRID_API_KEY=SG.replace-with-your-api-key
SENDGRID_FROM_EMAIL=verified-sender@example.com

# Optional. Defaults to the public portfolio email when omitted.
CONTACT_TO_EMAIL=Lenin9073@gmail.com
